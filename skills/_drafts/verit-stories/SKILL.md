---
name: verit-stories
description: Research and script new 'real world stories' episodes (stories-*.txt) for the Verit Analytics video series in the established format
---

<!-- reflection reason: Repeatable multi-step procedure (check existing stories → research new cases → script in a strict house format → verify word budget) that has now been run 8+ times and will recur; encoding the format rules and workflow saves re-deriving them. -->

1. Recall context: memory_search for 'real world stories Verit' and list ank-content/scripts/stories-*.txt plus output/stories-* to see which stories already exist. 2. Pick fresh real-world supply-chain cases not yet covered (candidates pool: 2021 chip shortage GM/Ford, Ever Given Suez, H&M 2018 excess inventory, Boeing, Lululemon sheer pants, etc.). 3. Verify facts with search_and_synthesize (one call per case): exact figures, dates, root cause, outcome numbers, citable sources. 4. Map each story to a Verit Analytics tool focus (Forecast Management/backtesting, Stockout Risk + Alerts, ABC/XYZ, Safety Stock/Reorder Points) so the lesson section pitches a different module per story. 5. Write scripts/stories-<slug>.txt in the series format: header comment (~90 sec, tool focus), [SCENE] blocks with layout (title/default/hub/table/bar_chart/cta), element/broll fields, on-screen cite line in bar_chart scenes, narration after each --- separator. 6. Keep narration ~530-600 words total (existing range 529-598); verify by splitting on '^---$' and counting words in parts[1:]. 7. Structure every story as: title hook → paradox/hook → event → divergence/cascade table → result bar chart with sources → lesson stat + Verit tie-in → CTA. 8. Report back with file paths, key verified stats, and offer next steps (render via verit-video-kit pipeline, YouTube metadata).
